# =============================================================================
# Reproducible Dependency Constraints (Python 3.10 & 3.11)
# =============================================================================
# These exact direct dependency versions are verified to pass all 52 headless
# unit tests, lint checks, and empirical benchmark suites in GitHub Actions CI
# (Ubuntu 24.04 runner) and Windows desktop environments.
#
# Usage:
#   pip install -r requirements.txt -r requirements-dev.txt -c constraints.txt
# =============================================================================

# Core Runtime Dependencies
opencv-python==4.10.0.84
mediapipe==0.10.14
numpy==1.26.4
sounddevice==0.5.1
scipy==1.14.1

# Development and Testing Tooling
pytest==8.3.4
ruff==0.9.9
